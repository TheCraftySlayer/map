__pycache__/
# Plaintext map body — never commit; re-encrypted into index_body.html.enc
index_body.html
index_body.html.bak
# Plaintext map data — never commit; re-encrypted into data/*.json.enc
data/core.json
data/layers.json
# OSRM drive-time cache — regenerated by build_data.py each run, not a
# source artifact. Avoids cluttering git status after every rebuild.
data/osrm_drive_times.json
