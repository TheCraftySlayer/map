__pycache__/
# Plaintext map body — never commit; re-encrypted into index_body.html.enc
index_body.html
index_body.html.bak
# Plaintext map data — never commit; re-encrypted into data/*.json.enc
data/core.json
data/layers.json
# ACS disk cache — re-fetched on demand; don't ship it in the repo
data/acs_cache/
# CHAS disk cache — same story
data/chas_cache/
# Operator scratch directory used by the Makefile (decrypt → patch → encrypt)
_work/
# Bundle from `make rotate-{staff,public}` — review then upload, don't commit
public_rotated/
